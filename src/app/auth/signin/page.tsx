import { Metadata } from "next";
import SignInPageClient from "./client";

export const metadata: Metadata = {
  title: "Sign In | Socio Engineer",
  description: "",
};

export default SignInPageClient;
